{
  "format": "snl-entry",
  "version": 1,
  "schema_version": 1,
  "package": "data-schema-spec",
  "entry": {
    "id": "schema.read.enumeration",
    "package": "data-schema-spec",
    "kind": "concept",
    "title": {
      "type": "i18n",
      "default_language": "en",
      "values": {
        "en": "Entity JSON enumeration and I/O",
        "zh-CN": "实体 JSON 枚举与 I/O"
      }
    },
    "content": {
      "markdown": "## Dashboard 调用边界\n\nDashboard 主体现在通过轻目录使用普通 Package/Library 读取适配器，不再依赖迁移适配器的全实体 snapshot。异步统计仍通过迁移存储适配器按批次读取 canonical Entry/Macro；两个适配器的枚举规则区别继续存在。以下“当前 Dashboard 刷新通常消费 snapshot”为重构前调用关系，不是现行开启合同。\n\nThe migration adapter `createVscodeDataMigrationStorage.listJsonFiles` selects regular files whose names end in `.json` case-insensitively, rejects any case-insensitive JSON-suffixed entry whose reported type is not `File`, and sorts selected names with `localeCompare`. Non-JSON entries are ignored.\n\nThe ordinary `snlDoc.ts` `entityReadStorage` is different: it selects only direct regular files with exact lowercase `.json`, silently ignoring uppercase `.JSON` names and non-file JSON-suffixed entries. A current Dashboard refresh normally consumes the operation snapshot built by the migration adapter, while direct Entry/Macro/Package readers may use the ordinary adapter. Conformance tests MUST name the adapter and API under test rather than assuming one enumeration rule.\n\nEvery selected file MUST remain present, be readable, and parse through a non-fatal UTF-8 `TextDecoder` followed by native `JSON.parse`. Malformed byte sequences can become replacement characters; duplicate JSON keys use JavaScript last-key-wins semantics. A disappearance or parse/I/O failure is fatal for the snapshot or inspection.\n"
    },
    "contribution_info": null,
    "pointer": null
  }
}
